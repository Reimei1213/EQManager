<%= form_with(model: equip, local: true) do |form| %>
  <% if equip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equip.errors.count, "error") %> prohibited this equip from being saved:</h2>

      <ul>
        <% equip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :classification, '分類' %>
    <%= form.collection_select :classification_id, Classification.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
